apiVersion: v1
kind: Service
metadata:
  name: payment-service
spec:
  selector:
    app.kubernetes.io/name: payment
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4005
  type: ClusterIP
