apiVersion: v1
kind: Service
metadata:
  name: notificataion-service
spec:
  selector:
    app.kubernetes.io/name: notificataion
  ports:
    - protocol: TCP
      port: 80
      targetPort: 4006
  type: ClusterIP
